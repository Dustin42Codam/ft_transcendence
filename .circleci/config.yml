version: 2.1

jobs:
  build:
    docker:
      - image: "lgutter/webserv_test:latest"
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.14
          docker_layer_caching: true
      - run:
          name: Build-app
          command: "./CiTests.sh"
      - run:
          name: "Say hello"
          command: "echo Hello, World!"