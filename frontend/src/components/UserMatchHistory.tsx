import React from "react";
import Wrapper from "./Wrapper";

function UserMatchHistory(props: { matchHistory: any }) {
  // let matches = match

  return (
    <div className="text-center">
      {/* add: display "no matches played" only when there are no matches  */}
      No matches played
    </div>
  );
}

export default UserMatchHistory;
